<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Listado de Estudiantes</title>
    <style>
        h1{
            font-size: 2rem;
            font-weight: 500;
            text-align: center;
            margin: 20px 0;
            font-family: Arial, sans-serif;
        }  
        h2{
            font-size: 1.5rem;
            font-weight: 500;
            text-align: center;
            margin: 20px 0;
            font-family: Arial, sans-serif;
        } 
        table{
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
            font-family: Arial, sans-serif;
        }
        th,td{
            padding: 12px;
            border: 1px solid #ddd;
            text-align: center;
            font-size: 1rem;
             font-family: Arial, sans-serif;
        }
        th{
            background-color: #f8f9fa;
            font-weight: 500;
        }
        tr:nth-child(even){
            background-color: #f2f2f2;
        }
        tr:hover{
            background-color: #f1f1f1;
        }
        .text-center{
            text-align: center;
        }
    </style>
</head>
<body>
    <h1>Listado de Estudiantes</h1>
    <h2>PDF con todos los estudiantes que estan Activos</h2>
    <table>
        <thead>
            <tr>
                <th>Nº Estudiante</th>
                <th>Nombre</th>
                <th>Salón</th>
                <th>Acudiente</th>
                <th>Edad</th>
                <th>Género</th>
            </tr>
        </thead>
        <tbody>
            {% for estudiante in estudiantes %}
                <tr>
                    <td>{{ estudiante.id }}</td>
                    <td>{{ estudiante.nombre }}</td>
                    <td>{{ estudiante.salon }}</td>
                    <td>{{ estudiante.acudiente }}</td>
                    <td>{{ estudiante.edad }}</td>
                    <td>{{ estudiante.genero }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>